# deploy.sh is a script to set the configuration for goinfre
# at school computers and clone repo from github. 
#
# WARNING before run:
# You need to replace dummy <intra_name> with you actual one on line 7
#  "data-root": "/goinfre/<intra_name>/.docker_data",
# (in `cat` block &(USER) or &(whoami) doesn't get expanded unfortunately)
#
# after executing the script you can acess repo in:
# /goinfre/<intra_name>/voice-chef
