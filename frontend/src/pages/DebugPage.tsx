import { DbModelsCard } from '@/components/debug/DbModelsCard';

/**
 * A page that simply renders the DbModelsCard component.
 * The layout is handled by the parent AppLayout.
 */
export function DebugPage() {
  return <DbModelsCard />;
}
