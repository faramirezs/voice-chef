# Backend API base URL
# Used when bypassing the Vite dev proxy (e.g. in production builds or standalone testing).
# In development the Vite proxy (vite.config.ts) forwards /api → http://fastapi:80,
# so this variable is not required locally.
VITE_API_URL=http://localhost:8000
